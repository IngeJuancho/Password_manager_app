<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema de splash al iniciar con modo claro del SO -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Tema aplicado en cuanto Flutter arranca -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!--
        FIX ANDROID 15/16: Tema transparente para AuthenticationActivity.
        Sin esto, el sistema clasifica la Activity como una interrupción del
        flujo de autofill y bloquea el PendingIntent — el botón se congela.
    -->
    <style name="Theme.Transparent" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>