<?xml version="1.0" encoding="utf-8"?>
<!--
    FIX ANDROID 15/16:
    RemoteViews se infla en un proceso de servicio sin Activity ni Theme Context.
    Los atributos ?android:attr/... (selectableItemBackground, colorAccent,
    textColorPrimary, textColorSecondary) no pueden resolverse en ese contexto
    y en Android 15/16 lanzan una excepción que mata el proceso completo.

    REGLA: En layouts usados por RemoteViews NUNCA usar ?attr/... ni @style/...
    Solo valores absolutos: colores hex, dimensiones fijas, drawables del sistema
    que no dependan de tema.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:gravity="center_vertical"
    android:background="#1A1A2E">

    <!-- Ícono candado — sin tint por atributo de tema -->
    <ImageView
        android:id="@+id/autofill_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@android:drawable/ic_lock_lock"
        android:contentDescription="Autocompletado" />

    <!-- Textos -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginLeft="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/autofill_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1" />

        <TextView
            android:id="@+id/autofill_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#AAAAAA"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1" />

    </LinearLayout>

</LinearLayout>